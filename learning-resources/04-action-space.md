# Constrain the Action Space

Question: Which actions can change the outcome in a valid workflow?

Practice: Name actions and their preconditions; reject invalid actions deterministically.

Evidence: A small table of valid, invalid, and no-op actions.

Avoid: Leaving tool affordances implicit, which makes failures impossible to classify.
